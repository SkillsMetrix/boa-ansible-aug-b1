
- name: read the data from var and set it to target
  copy:
    content: "{{message}}"
    dest: /var/www/html/index.html





- name: install required packages
  apt:
    - nginx
    - git
    - curl
  state: latest
  update_cache: true
